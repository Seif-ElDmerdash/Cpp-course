#include <iostream>
using namespace std;

int main()
{

    int number, res;
    cin>>res;
    cin >> number; if (number > res) {res = number;}
    cin >> number; if (number > res) {res = number;}
    cin >> number; if (number > res) {res = number;}
    cin >> number; if (number > res) {res = number;}
    cin >> number; if (number > res) {res = number;}
    cin >> number; if (number > res) {res = number;}
    cin >> number; if (number > res) {res = number;}
    cin >> number; if (number > res) {res = number;}
    cin >> number; if (number > res) {res = number;}

    cout<<res<<endl;

  
    return 0;
}